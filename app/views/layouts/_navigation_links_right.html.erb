<%if @btce_buy && @btce_sell %>
  <li>
    <%= link_to "BTC Buy: $#{@btce_buy} \t Sell: $#{@btce_sell}" %>
  </li>
<% end %>
<% if @credentials %>
  <% if @info %>
    <li>
      <%= link_to @info %>
    </li>
  <% end %>
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= @credentials[:apikey][0,8] %> <b class="caret"></b></a>
    <ul class="dropdown-menu">
      <li><%= link_to "logout", logout_path %></li>
    </ul>
  </li>
<% end %>
