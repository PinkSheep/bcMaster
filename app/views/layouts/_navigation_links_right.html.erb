<%if @btce_buy && @btce_sell %>
  <li>
    <p class="navbar-text"><%= "BTC Buy: $#{@btce_buy} Sell: $#{@btce_sell}" %></p>
  </li>
<% end %>
<% if @credentials %>
  <% if @info %>
    <li>
      <p class="navbar-text"><%= @info %></p>
    </li>
  <% end %>
  <li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= @credentials[:apikey] %> <b class="caret"></b></a>
    <ul class="dropdown-menu">
      <li><%= link_to "Logout", logout_path %></li>
    </ul>
  </li>
<% end %>
